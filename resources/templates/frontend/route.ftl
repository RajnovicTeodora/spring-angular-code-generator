import { Routes } from '@angular/router';
import { ${class.name}Component } from './${class.name}/${class.name?uncap_first}.component';

export const GRADE_ROUTES: Routes = [{ path: '', component: GradesComponent }];
